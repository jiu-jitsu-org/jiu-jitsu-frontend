import { DetailPlaygroundListPage } from "./detail-playground-list-page";

/**
 * 게시글 상세 쇼케이스 중간 페이지 경로(`/community-playground/detail`).
 *
 * 타입별 상세 화면으로 가는 리스트를 보여준다(곧바로 상세를 띄우지 않음).
 */
export default function CommunityDetailPlaygroundList() {
  return <DetailPlaygroundListPage />;
}
